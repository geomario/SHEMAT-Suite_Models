Compilation
---------------------------

Use the shell-script compilequick.sh and specify the corresponding
model directory and SHEMAT-Suite directory inside the script.

Should be compiled with the EnKF version of the SHEMAT-Code found in
the branch "enkf".

Typical compilation command:

gmake sm COMPTYPE=ling64 PROPS=const omp noplt vtk nohdf -j 16

