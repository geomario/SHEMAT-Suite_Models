import unittest

try:
    import test_fw_const_TheisProblem
except ImportError:
    raise ImportError(
        'No module named test_fw_const_TheisProblem. \n\n' + 'Solution: ' +
        ' Add the path /path-to-SHEMAT-Suite_Models/' +
        'SHEMAT-Suite_Models/deltatests/site-packages to the' + ' PYTHONPATH.')
try:
    import test_fw_basc_SaltFlow
except ImportError:
    raise ImportError(
        'No module named test_fw_basc_SaltFlow. \n\n' + 'Solution: ' +
        ' Add the path /path-to-SHEMAT-Suite_Models/' +
        'SHEMAT-Suite_Models/deltatests/site-packages to the' + ' PYTHONPATH.')

try:
    import test_fw_basc_SaltFlow_small
except ImportError:
    raise ImportError(
        'No module named test_fw_basc_SaltFlow_small. \n\n' + 'Solution: ' +
        ' Add the path /path-to-SHEMAT-Suite_Models/' +
        'SHEMAT-Suite_Models/deltatests/site-packages to the' + ' PYTHONPATH.')

try:
    import test_ad_const_TemperatureInversion
except ImportError:
    raise ImportError(
        'No module named test_ad_const_TemperatureInversion. \n\n' +
        'Solution: ' + ' Add the path /path-to-SHEMAT-Suite_Models/' +
        'SHEMAT-Suite_Models/deltatests/site-packages to the' + ' PYTHONPATH.')

try:
    import test_fw_bas_TinyAdvective
except ImportError:
    raise ImportError(
        'No module named test_fw_bas_TinyAdvective. \n\n' + 'Solution: ' +
        ' Add the path /path-to-SHEMAT-Suite_Models/' +
        'SHEMAT-Suite_Models/deltatests/site-packages to the' + ' PYTHONPATH.')

try:
    import test_fw_const_Peclet_down
except ImportError:
    raise ImportError(
        'No module named test_fw_const_Peclet_down. \n\n' + 'Solution: ' +
        ' Add the path /path-to-SHEMAT-Suite_Models/' +
        'SHEMAT-Suite_Models/deltatests/site-packages to the' + ' PYTHONPATH.')

try:
    import test_fw_const_Peclet_up
except ImportError:
    raise ImportError(
        'No module named test_fw_const_Peclet_up. \n\n' + 'Solution: ' +
        ' Add the path /path-to-SHEMAT-Suite_Models/' +
        'SHEMAT-Suite_Models/deltatests/site-packages to the' + ' PYTHONPATH.')

try:
    import test_fw_basc_HenryProblem
except ImportError:
    raise ImportError(
        'No module named test_fw_basc_HenryProblem. \n\n' + 'Solution: ' +
        ' Add the path /path-to-SHEMAT-Suite_Models/' +
        'SHEMAT-Suite_Models/deltatests/site-packages to the' + ' PYTHONPATH.')

try:
    import test_fw_kola_HeatConduction2D
except ImportError:
    raise ImportError(
        'No module named test_fw_kola_HeatConduction2D. \n\n' + 'Solution: ' +
        ' Add the path /path-to-SHEMAT-Suite_Models/' +
        'SHEMAT-Suite_Models/deltatests/site-packages to the' + ' PYTHONPATH.')


def full_suite():
    full_suite = unittest.TestSuite()

    full_suite.addTest(test_fw_const_TheisProblem.local_suite())

    full_suite.addTest(test_fw_basc_SaltFlow.local_suite())

    full_suite.addTest(test_fw_basc_SaltFlow_small.local_suite())

    full_suite.addTest(test_ad_const_TemperatureInversion.local_suite())

    full_suite.addTest(test_fw_bas_TinyAdvective.local_suite())

    full_suite.addTest(test_fw_const_Peclet_down.local_suite())

    full_suite.addTest(test_fw_const_Peclet_up.local_suite())

    full_suite.addTest(test_fw_basc_HenryProblem.local_suite())

    full_suite.addTest(test_fw_kola_HeatConduction2D.local_suite())

    return full_suite


if __name__ == "__main__":
    runner = unittest.TextTestRunner(verbosity=2)
    runner.run(full_suite())
